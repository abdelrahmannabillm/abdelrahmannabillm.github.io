Drop your profile photo here as: profile.jpg

The site looks for /assets/img/profile.jpg in index.html.
If the file is missing, the hero shows a clean "AN" initials avatar
(no broken image icon — handled by JS fallback).

Recommended:
- Square crop (the photo is rendered as a 300px circle)
- At least 600 x 600 px
- JPG or PNG (rename to profile.jpg either way)
